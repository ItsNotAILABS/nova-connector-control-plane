{
  "schema": "mcp.connector_control_plane.server.v1",
  "name": "NOVA Connector Control Plane",
  "description": "MCP-style contract for connector discovery, route planning, and artifact import receipts.",
  "resources": [
    {
      "uri": "nova://connectors/registry",
      "name": "Connector Registry",
      "path": "data/connectors.json"
    }
  ],
  "tools": [
    {
      "name": "connectors.list",
      "input_schema": {},
      "output": "List registered connectors."
    },
    {
      "name": "connectors.describe",
      "input_schema": {"connector_id": "string"},
      "output": "Return connector authority, inputs, outputs, and proof gates."
    },
    {
      "name": "connectors.route_plan",
      "input_schema": {"task": "string", "target_surface": "string", "risk_level": "string"},
      "output": "Return connector route plan and proof gates."
    },
    {
      "name": "connectors.import_artifact",
      "input_schema": {"connector_id": "string", "artifact_url": "string", "sha256": "string", "validation_status": "string"},
      "output": "Return artifact import receipt."
    }
  ]
}
